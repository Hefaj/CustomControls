<Window x:Class="CustomControls.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CustomControls"
        xmlns:custom="clr-namespace:CustomControls.custom"
        mc:Ignorable="d"
        Name="mainWiondow"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <StackPanel>
            <custom:MagicBox
                SelectedItemsList="{Binding Selected, ElementName=mainWiondow}"
                ItemSoruce2="{Binding Selected, ElementName=mainWiondow, Mode=TwoWay}"
                ItemSoruce="{Binding Persons, ElementName=mainWiondow}"/>
          <Button Content="Sprawdz" Click="ButtonBase_OnClick"/>  
            
        </StackPanel>
    </Grid>
</Window>
